services:
  db:
    build: database
    ports:
      - 3306:3306
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      - MYSQL_DATABASE=film
      - MYSQL_ROOT_PASSWORD=film
    expose:
      - 3306
      - 33060

volumes:
  mysql_data: